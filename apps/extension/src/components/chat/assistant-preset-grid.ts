/**
 * 说明：`assistant-preset-grid` 共享布局模块。
 *
 * 职责：
 * - 收口内置助手卡片在轻量选择弹窗与助手商店里的静态网格类名；
 * - 避免两处继续各自维护一套看起来接近、实际会漂移的布局契约；
 * - 让“默认助手作为通用分区第一张紧凑 tile + 两列轻量网格”成为稳定真源。
 *
 * 边界：
 * - 本模块只提供静态 class 契约；
 * - 不处理虚拟列表、测宽或预设卡片本身的渲染逻辑。
 */

/**
 * 内置通用助手常规规模静态网格。
 *
 * @remarks
 * 当前产品希望它与轻量角色选择弹窗保持同一视觉密度：
 * 默认单列，小屏以上稳定两列，不再依赖运行时测宽。
 */
export const ASSISTANT_GENERAL_STATIC_GRID_CLASS = 'grid grid-cols-1 gap-2 pb-2 sm:grid-cols-2';
