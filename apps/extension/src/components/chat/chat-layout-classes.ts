/**
 * 说明：主聊天阅读列布局常量模块。
 *
 * 职责：
 * - 收口主聊天普通对话内容与输入区共享的可读宽度；
 * - 保持虚拟滚动技术容器可以铺满视口，同时让用户真正阅读和输入的内容落在同一列。
 *
 * 边界：
 * - 这里只声明布局 class 真源，不承载滚动、消息渲染或输入区交互逻辑。
 */

/**
 * 主聊天普通阅读列宽度。
 *
 * @remarks
 * 聊天滚动视口仍可铺满宿主容器，但消息、上下文分隔线、加载态和 composer
 * 共享这条 `64rem` 阅读列，避免宽屏下出现“外壳铺满、内容无限变宽”的失控观感。
 */
export const CHAT_READING_COLUMN_CLASS = 'mx-auto w-full max-w-[64rem]';
